<p>
  <%= content_tag(:span ,:class => 'rp-fnamerankicon') do%>
  <%= current_user.fname%>
  <%= image_tag current_user.rankicon, height: '35', width: '35'  %>
<%end%>
</p>
<%= image_tag current_user.avatar.url(:thumb), :class=> 'rp-avatar' %>

<span><%= link_to "view my profile", current_user %></span>
<br>
<span><%= pluralize(current_user.reviews.count, "review") %></span>
<br>
